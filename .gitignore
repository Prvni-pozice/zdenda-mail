# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Citlivá data — heslo se NIKDE neukládá, ale .env může obsahovat IMAP_USER
.env

# SQLite databáze (+ WAL, shared memory)
*.db
*.db-wal
*.db-shm
# zálohy DB — i varianty jako .bak-v4, .bak2, .bak-v6-pre
*.bak*

# IMAP zálohy
*.mbox

# Stažené přílohy (faktury) — drží se mimo Git
invoices/

# Transientní dávky pro LLM re-triage + generované reporty
scripts/unsure_batches/
open_loops.md

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Transientní reapply plán
reapply_plan.json
